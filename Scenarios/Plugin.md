# Plugin

## Open Plugin page

If the user is signed in and opens the Plugin page, then the page shows plugin-related content (for example integration instructions, API keys, or links to install a plugin). The exact content depends on implementation. The user can use this to integrate the booking system with other tools.

## Not signed in

If the user is not signed in and tries to open the Plugin page, then the app may redirect them to the login page.

namespace CargoHub.Application.Auth;

/// <summary>User-facing strings for auth flows (login, portal, middleware).</summary>
public static class AuthMessages
{
    public const string CompanyInactive =
        "This company is inactive. Contact a Super Admin to resolve the issue.";
}
